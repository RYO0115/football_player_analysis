# データ保存ディレクトリ (省略時 data)
FPA_DATA_DIR=data

# false にすると Substack へ実際に投稿する。既定は true (ローカル出力のみ)
FPA_DRY_RUN=true

# Substack 認証情報 (非公式 API はメール/パスワード認証)
SUBSTACK_EMAIL=your-email@example.com
SUBSTACK_PASSWORD=your-password
SUBSTACK_PUBLICATION_URL=https://yourname.substack.com
